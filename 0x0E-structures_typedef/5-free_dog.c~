#include "dog.h"
#include <stdlib.h>
/**
 *free_dog - free dog function
 *@d: dog struct
 *Return: void
 */
void free_dog(dog_t *d)
{
if (d->name != NULL)
free(d->name);
if (d->owner != NULL)
free(d->owner);
d->age == NULL;
free(d);
}
