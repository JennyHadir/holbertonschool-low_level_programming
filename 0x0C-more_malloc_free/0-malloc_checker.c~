#include "holberton.h"
/**
 *malloc_checked - malloc function
 *
 *@b: unsigned int
 *Return: void
 */
void *malloc_cheked(unsigned int b)
{
char *str;
str = malloc(b);
if (str == 0)
{
exit(98);
return (NULL);
}
return (str);
}
