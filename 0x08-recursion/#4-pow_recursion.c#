#include "holberton.h"
/**
 * _pow_recursion - power function
 *
 * @x: integer
 * @y: integer
 * Return: integer
 */
int _pow_recursion(int x, int y)
{
  if (y == 0)
{
  return(1);
}
  else if (y < 0)
{
return ();
}
else
{
  return (x * x);
  _pow_recursion(x, y - 1);
}
}
