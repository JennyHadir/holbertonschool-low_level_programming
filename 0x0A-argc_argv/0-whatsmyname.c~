#include <stdio.h>
/**
 * main - main function
 *
 * @i: integer, first argument of argv
 * @argv: array contains the argument value
 * Return: 0
 */
int main(int i, char **argv)
{
i = 0;
printf("%s\n", argv[i]);
return (0);
}
