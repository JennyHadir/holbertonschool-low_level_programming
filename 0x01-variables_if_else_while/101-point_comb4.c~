#include <stdio.h>

/**
 *main - print the numbers from 00 to 99.
 *
 *Return: 0 (success)
 */

int main(void)
{

	int x;
	int y;
	int z;

	for (x = 48; x < 58; x++)
	{
		for (y = 48; y < 58; y++)
		{
			for (z = 48; z < 58; z++)
			{
				if ((x > y) && (y > z))
				{
					putchar(x);
					putchar(y);
					if (x == 55)
					{
						if (y == 56 )
						{
							if (z == 57)
							{
								putchar(',');
								putchar(' ');
							}
						}
					}
				}
			}
		}
	}
		putchar('\n');
		return (0);
}
