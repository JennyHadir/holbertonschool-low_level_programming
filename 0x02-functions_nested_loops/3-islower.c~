#include "holberton.h"

/**
 * _islower - function that return 1 if the char is lowercase and 0 otherwise
 *
 * @r: parametre
 * Return: 0 or 1
 */
int _islower(int r)
{
	if (r >= 'a' && r <= 'z')
	{
		return (1);
	}
	else if (r >= 'A' && r <= 'Z')
	{
		return (0);
	}
}
